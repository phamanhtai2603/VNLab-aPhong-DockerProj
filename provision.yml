- hosts:
    - local_ci
  remote_user: operator_user
  connection: local
  roles:
    - app.common
    - geerlingguy.git
    - geerlingguy.docker

- hosts:
    - local_app
  remote_user: operator_user
  roles:
    - app.common
    - geerlingguy.git
    - geerlingguy.docker

# Already installed in hosts
# - hosts: all
#   roles:
#     - weareinteractive.docker-compose
#   vars:
#     docker_compose_version: 1.21.0
#     docker_compose_pip_version: pip #pip==9.0.3
